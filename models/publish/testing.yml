version: 2
models:
  - name: customer_orders
    description: "{{ doc('PUBLISH_docs') }}"
    database: DBTTRAINING
    schema: PUBLISH
    columns:
      - name: C_CUSTKEY
        tests:
          - not_null
      - name: O_TOTALPRICE
        tests:
          - not_null